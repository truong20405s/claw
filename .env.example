# Optional: Proxy server / Proxy pool to bypass Cloud / Datacenter IP blocks (e.g. Railway, VPS)
# Provide a single proxy or comma-separated list for automatic fallback rotation:
# PROXY_POOL=socks5://host1:port,socks5://host2:port
# PROXY_SERVER=socks5://host:port
